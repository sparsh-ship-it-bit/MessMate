import React,{useEffect,useMemo,useState} from 'react';
import {createRoot} from 'react-dom/client';
import {Html5Qrcode} from 'html5-qrcode';
import './styles.css';
const API=import.meta.env.VITE_API_URL||'';const meals=['breakfast','lunch','dinner'];const today=()=>new Date().toISOString().slice(0,10);const label=p=>p==='all'?'All meals':(p||'').split(',').filter(Boolean).map(x=>x[0].toUpperCase()+x.slice(1)).join(' + ');
async function api(path,opts={}){const token=localStorage.getItem('messmate_token');const headers={'Content-Type':'application/json',...(opts.headers||{})};if(token)headers.Authorization=`Bearer ${token}`;const r=await fetch(API+path,{...opts,headers});const t=await r.text();let d={};try{d=t?JSON.parse(t):{}}catch{d={error:t}}if(!r.ok)throw Error(d.error||`Request failed (${r.status})`);return d}
function Field({label:txt,children}){return <label>{txt}{children}</label>}function Modal({title,close,children}){return <div className="modal-backdrop" onClick={close}><div className="modal" onClick={e=>e.stopPropagation()}><div className="panel-head"><h3>{title}</h3><button type="button" className="close" onClick={close}>×</button></div>{children}</div></div>}
function Auth({onLogin}){const[mode,setMode]=useState('login'),[f,setF]=useState({name:'',email:'',phone:'',password:''}),[err,setErr]=useState('');const submit=async e=>{e.preventDefault();setErr('');try{const d=await api(`/api/v1/auth/${mode}`,{method:'POST',body:JSON.stringify(f)});localStorage.setItem('messmate_token',d.token);onLogin()}catch(e){setErr(e.message)}};return <main className="auth-shell"><section className="auth-card"><div className="brand"><span className="logo">M</span><div><b>MessMate</b><small>Mess management, simplified.</small></div></div><h1>{mode==='login'?'Welcome back':'Create owner account'}</h1><p className="muted">Run consumers, meals, subscriptions and collections from one console.</p><form onSubmit={submit}>{mode==='register'&&<><Field label="Name"><input required value={f.name} onChange={e=>setF({...f,name:e.target.value})}/></Field><Field label="Phone"><input value={f.phone} onChange={e=>setF({...f,phone:e.target.value})}/></Field></>}<Field label="Email"><input required type="email" value={f.email} onChange={e=>setF({...f,email:e.target.value})}/></Field><Field label="Password"><input required minLength="8" type="password" value={f.password} onChange={e=>setF({...f,password:e.target.value})}/></Field>{err&&<div className="error">{err}</div>}<button className="primary full">{mode==='login'?'Sign in':'Create account'}</button></form><button className="link" onClick={()=>{setMode(mode==='login'?'register':'login');setErr('')}}>{mode==='login'?'New to MessMate? Create an account':'Already have an account? Sign in'}</button></section></main>}
function App(){const[logged,setLogged]=useState(!!localStorage.getItem('messmate_token'));return logged?<Console logout={()=>{localStorage.removeItem('messmate_token');setLogged(false)}}/>:<Auth onLogin={()=>setLogged(true)}/>}
function Console({logout}){const[tab,setTab]=useState('dashboard'),[stats,setStats]=useState(null),[consumers,setConsumers]=useState([]),[pending,setPending]=useState([]),[attendance,setAttendance]=useState([]),[expiring,setExpiring]=useState([]),[notice,setNotice]=useState(''),[modal,setModal]=useState(null),[edit,setEdit]=useState(null),[query,setQuery]=useState('');const load=async()=>{try{const[s,c,p,a,x]=await Promise.all([api('/api/v1/dashboard'),api('/api/v1/consumers'),api('/api/v1/payments/pending'),api('/api/v1/attendance/today'),api('/api/v1/subscriptions/expiring-today')]);setStats(s);setConsumers(c);setPending(p);setAttendance(a);setExpiring(x)}catch(e){setNotice(e.message)}};useEffect(()=>{load()},[]);const active=useMemo(()=>consumers.filter(c=>c.active),[consumers]);const filtered=useMemo(()=>consumers.filter(c=>`${c.name} ${c.consumer_id} ${c.phone}`.toLowerCase().includes(query.toLowerCase())),[consumers,query]);const saveConsumer=async f=>{try{let id=edit?.id;if(id){await api(`/api/v1/consumers/${id}`,{method:'PATCH',body:JSON.stringify({name:f.name,phone:f.phone,meal_plan:f.meal_plan})})}else{const c=await api('/api/v1/consumers',{method:'POST',body:JSON.stringify({name:f.name,phone:f.phone,consumer_id:f.consumer_id,meal_plan:f.meal_plan})});id=c.id;const sub=await api('/api/v1/subscriptions',{method:'POST',body:JSON.stringify({consumer_id:id,start_date:f.start_date,end_date:f.end_date,amount:Number(f.amount),amount_paid:Number(f.amount_paid||0),month_label:new Date(f.start_date+'T00:00:00').toLocaleDateString('en-IN',{month:'long',year:'numeric'}),payment_status:Number(f.amount_paid||0)>=Number(f.amount)?'paid':Number(f.amount_paid||0)>0?'partial':'pending'})});if(Number(f.amount_paid||0)>0)await api('/api/v1/payments',{method:'POST',body:JSON.stringify({consumer_id:id,subscription_id:sub.id,amount:Number(f.amount_paid),method:'cash'})})}setModal(null);setEdit(null);setNotice(edit?'Consumer updated':'Consumer added successfully');await load()}catch(e){setNotice(`Could not save consumer: ${e.message}`)}};const remove=async c=>{if(!confirm(`Delete ${c.name}?`))return;try{await api(`/api/v1/consumers/${c.id}`,{method:'DELETE'});setNotice('Consumer deleted');load()}catch(e){setNotice(e.message)}};return <div className="app"><aside><div className="brand side"><span className="logo">M</span><div><b>MessMate</b><small>Owner console</small></div></div><nav>{[['dashboard','Overview'],['consumers','Consumers'],['subscriptions','Subscriptions'],['attendance','Attendance'],['payments','Payments'],['reports','Reports']].map(([id,l])=><button className={tab===id?'nav active':'nav'} onClick={()=>setTab(id)} key={id}>{l}</button>)}</nav><button className="logout" onClick={logout}>Sign out</button></aside><section className="content"><header><div><p className="eyebrow">MESS MANAGEMENT</p><h2>{tab==='dashboard'?'Good morning 👋':tab[0].toUpperCase()+tab.slice(1)}</h2></div>{tab!=='reports'&&<button className="primary" onClick={()=>{setEdit(null);setModal('consumer')}}>+ Add consumer</button>}</header>{notice&&<div className="notice" onClick={()=>setNotice('')}>{notice}</div>}{tab==='dashboard'&&<Dashboard stats={stats} consumers={consumers} expiring={expiring} openTab={setTab} qr={c=>setModal({type:'qr',consumer:c})}/>} {tab==='consumers'&&<section className="panel"><div className="panel-head"><h3>Consumers ({active.length} active)</h3><input className="search" placeholder="Search name, ID or phone" value={query} onChange={e=>setQuery(e.target.value)}/></div>{filtered.map(c=><ConsumerRow key={c.id} c={c} qr={()=>setModal({type:'qr',consumer:c})} edit={()=>{setEdit(c);setModal('consumer')}} remove={()=>remove(c)}/>)}{!filtered.length&&<Empty text="No consumers found."/>}</section>} {tab==='subscriptions'&&<Subscriptions consumers={consumers} openNew={()=>{setEdit(null);setModal('subscription')}} renew={c=>{setEdit(c);setModal('subscription')}}/>} {tab==='attendance'&&<Attendance attendance={attendance} onScan={async(token,meal)=>{try{await api('/api/v1/attendance/scan',{method:'POST',body:JSON.stringify({qr_token:token,meal})});setNotice('Attendance marked');load()}catch(e){setNotice(e.message)}}}/>} {tab==='payments'&&<Payments pending={pending} open={()=>setModal('payment')}/>} {tab==='reports'&&<Reports stats={stats} consumers={consumers} pending={pending} attendance={attendance}/>}</section>{modal==='consumer'&&<ConsumerModal initial={edit} close={()=>{setModal(null);setEdit(null)}} save={saveConsumer}/>} {modal==='subscription'&&<SubscriptionModal consumer={edit} consumers={consumers} close={()=>{setModal(null);setEdit(null)}} save={async f=>{try{await api('/api/v1/subscriptions',{method:'POST',body:JSON.stringify(f)});setNotice('Subscription saved');setModal(null);setEdit(null);load()}catch(e){setNotice(e.message)}}}/>} {modal==='payment'&&<PaymentModal consumers={consumers} close={()=>setModal(null)} save={async f=>{try{await api('/api/v1/payments',{method:'POST',body:JSON.stringify(f)});setNotice('Payment recorded');setModal(null);load()}catch(e){setNotice(e.message)}}}/>} {modal?.type==='qr'&&<QRModal c={modal.consumer} close={()=>setModal(null)}/>}</div>}
function ConsumerRow({c,qr,edit,remove}){return <div className="row consumer"><div className="avatar">{c.name?.[0]?.toUpperCase()}</div><div className="grow"><b>{c.name}</b><small>{c.consumer_id} · {c.phone} · {label(c.meal_plan)}</small></div><span className={c.active?'badge ok':'badge'}>{c.active?'Active':'Inactive'}</span><button className="ghost" onClick={qr}>QR</button><button className="ghost" onClick={edit}>Edit</button><button className="danger" onClick={remove}>Delete</button></div>}
function Dashboard({stats,consumers,expiring,openTab,qr}){return <><div className="stats">{[['Active consumers',stats?.active_consumers??'—'],['Expiring today',stats?.expiring_today??'—'],['Pending payments',stats?.pending_payments??'—'],['Today collections',stats?.today_collections!=null?`₹${stats.today_collections}`:'—'],['Today attendance',stats?.today_attendance??'—']].map(([l,v])=><div className="stat" key={l}><span>{l}</span><strong>{v}</strong></div>)}</div><div className="grid"><section className="panel"><div className="panel-head"><h3>Expiring today</h3><button className="link" onClick={()=>openTab('subscriptions')}>View subscriptions</button></div>{expiring.slice(0,6).map(c=><div className="row" key={c.id}><div className="grow"><b>{c.name}</b><small>{c.phone} · ₹{c.amount} · {c.payment_status}</small></div></div>)}{!expiring.length&&<Empty text="Nothing expiring today."/>}</section><section className="panel"><div className="panel-head"><h3>Recent consumers</h3><button className="link" onClick={()=>openTab('consumers')}>View all</button></div>{consumers.slice(0,6).map(c=><div className="row" key={c.id}><div className="avatar">{c.name?.[0]?.toUpperCase()}</div><div className="grow"><b>{c.name}</b><small>{c.consumer_id} · {label(c.meal_plan)}</small></div><button className="ghost" onClick={()=>qr(c)}>QR</button></div>)}{!consumers.length&&<Empty text="Add your first consumer."/>}</section></div></>}
function Subscriptions({consumers,openNew,renew}){return <section className="panel"><div className="panel-head"><h3>Subscriptions</h3><button className="primary small" onClick={openNew}>+ New subscription</button></div>{consumers.map(c=><div className="row" key={c.id}><div className="grow"><b>{c.name}</b><small>{c.consumer_id} · {label(c.meal_plan)} · {c.subscription?.end_date&&c.subscription.end_date!=='0001-01-01'?`Ends ${c.subscription.end_date}`:'No subscription'}</small></div><span className={c.subscription?.payment_status==='paid'?'badge ok':'badge pending'}>{c.subscription?.payment_status||'pending'}</span><button className="ghost" onClick={()=>renew(c)}>Renew</button></div>)}</section>}
function Attendance({attendance,onScan}){const[meal,setMeal]=useState('lunch'),[token,setToken]=useState(''),[scanner,setScanner]=useState(null);const start=async()=>{const q=new Html5Qrcode('messmate-reader');setScanner(q);try{await q.start({facingMode:'environment'},{fps:10,qrbox:240},text=>{q.stop().catch(()=>{});setScanner(null);onScan(text,meal)},()=>{})}catch{setScanner(null)}};return <div className="grid"><section className="panel scanner-panel"><h3>Meal attendance</h3><div className="meal-tabs">{meals.map(m=><button type="button" className={meal===m?'selected':''} onClick={()=>setMeal(m)} key={m}>{m}</button>)}</div><div id="messmate-reader"></div>{!scanner?<button className="primary full" onClick={start}>Open camera scanner</button>:<button className="ghost full" onClick={()=>{scanner.stop().catch(()=>{});setScanner(null)}}>Stop camera</button>}<div className="divider">or enter QR token</div><input value={token} onChange={e=>setToken(e.target.value)} placeholder="QR token UUID"/><button className="primary full" disabled={!token} onClick={()=>{onScan(token,meal);setToken('')}}>Mark attendance</button></section><section className="panel"><h3>Today's attendance ({attendance.length})</h3>{attendance.map((a,i)=><div className="row" key={i}><div className="grow"><b>{a.name||a.consumer_id}</b><small>{a.meal} · {a.scanned_at?new Date(a.scanned_at).toLocaleTimeString():''}</small></div><span className="badge ok">Present</span></div>)}{!attendance.length&&<Empty text="Nothing scanned yet."/>}</section></div>}
function Payments({pending,open}){return <section className="panel"><div className="panel-head"><h3>Pending payments ({pending.length})</h3><button className="primary small" onClick={open}>+ Record payment</button></div>{pending.map((p,i)=><div className="row" key={i}><div className="grow"><b>{p.name}</b><small>{p.consumer_id} · Due {p.end_date} · ₹{p.amount}</small></div><span className="badge pending">{p.payment_status}</span></div>)}{!pending.length&&<Empty text="No pending payments."/>}</section>}
function Reports({stats,consumers,pending,attendance}){const paid=consumers.filter(c=>c.subscription?.payment_status==='paid').length;const plans=consumers.reduce((m,c)=>(m[c.meal_plan]=(m[c.meal_plan]||0)+1,m),{});return <div className="grid"><section className="panel"><h3>Operations summary</h3><div className="report-grid"><div><span>Consumers</span><b>{consumers.length}</b></div><div><span>Active</span><b>{stats?.active_consumers??0}</b></div><div><span>Paid plans</span><b>{paid}</b></div><div><span>Pending</span><b>{pending.length}</b></div><div><span>Today's meals</span><b>{attendance.length}</b></div><div><span>Today's collections</span><b>₹{stats?.today_collections??0}</b></div></div></section><section className="panel"><h3>Meal plans</h3>{Object.entries(plans).map(([p,n])=><div className="row" key={p}><div className="grow"><b>{label(p)}</b></div><span className="badge">{n}</span></div>)}</section></div>}
function ConsumerModal({initial,close,save}){const initialMeals=initial?.meal_plan?(initial.meal_plan==='all'?meals:initial.meal_plan.split(',').filter(Boolean)):meals;const[f,setF]=useState({name:initial?.name||'',phone:initial?.phone||'',consumer_id:initial?.consumer_id||'',start_date:initial?.subscription?.start_date&&initial.subscription.start_date!=='0001-01-01'?initial.subscription.start_date:today(),end_date:initial?.subscription?.end_date&&initial.subscription.end_date!=='0001-01-01'?initial.subscription.end_date:'',amount:initial?.subscription?.amount||'',amount_paid:initial?.subscription?.amount_paid??''});const[selected,setSelected]=useState(initialMeals);const[touched,setTouched]=useState(false);const toggle=m=>setSelected(v=>v.includes(m)?v.filter(x=>x!==m):[...v,m]);const all=selected.length===3;const submit=e=>{e.preventDefault();setTouched(true);if(!selected.length||!f.name||!f.phone||!f.start_date||!f.end_date||f.amount===''||Number(f.amount_paid||0)>Number(f.amount))return;save({...f,meal_plan:all?'all':selected.join(',')})};return <Modal title={initial?'Edit consumer':'Add consumer'} close={close}><form onSubmit={submit}><Field label="Name"><input required value={f.name} onChange={e=>setF({...f,name:e.target.value})}/></Field><Field label="Phone"><input required value={f.phone} onChange={e=>setF({...f,phone:e.target.value})}/></Field>{!initial&&<Field label="Consumer ID"><input value={f.consumer_id} onChange={e=>setF({...f,consumer_id:e.target.value})} placeholder="Auto-generated if empty"/></Field>}<Field label="Meals"><div className="meal-checks">{meals.map(m=><label className="check" key={m}><input type="checkbox" checked={selected.includes(m)} onChange={()=>toggle(m)}/><span>{m[0].toUpperCase()+m.slice(1)}</span></label>)}</div><button type="button" className="link" onClick={()=>setSelected(all?[]:meals)}>{all?'Clear meals':'Select all meals'}</button></Field><div className="form-section"><b>Subscription & payment</b><Field label="Start date"><input required type="date" value={f.start_date} onChange={e=>setF({...f,start_date:e.target.value})}/></Field><Field label="End date"><input required type="date" min={f.start_date} value={f.end_date} onChange={e=>setF({...f,end_date:e.target.value})}/></Field><Field label="Total amount"><input required type="number" min="0" step="0.01" value={f.amount} onChange={e=>setF({...f,amount:e.target.value})}/></Field><Field label="Amount paid"><input required type="number" min="0" step="0.01" value={f.amount_paid} onChange={e=>setF({...f,amount_paid:e.target.value})}/></Field>{touched&&(!selected.length||!f.name||!f.phone||!f.start_date||!f.end_date||f.amount===''||Number(f.amount_paid||0)>Number(f.amount))&&<div className="error">Please complete all fields and make sure amount paid is not greater than total amount.</div>}</div><button type="submit" className="primary full">Save consumer</button></form></Modal>}
function SubscriptionModal({consumer,consumers,close,save}){const initial=consumer?.subscription;const[f,setF]=useState({consumer_id:consumer?.id||consumers[0]?.id||'',start_date:initial?.start_date&&initial.start_date!=='0001-01-01'?initial.start_date:today(),end_date:initial?.end_date&&initial.end_date!=='0001-01-01'?initial.end_date:'',amount:initial?.amount||'',month_label:initial?.month_label||'',payment_status:initial?.payment_status||'pending'});return <Modal title={consumer?'Renew subscription':'New subscription'} close={close}><form onSubmit={e=>{e.preventDefault();save(f)}}><Field label="Consumer"><select value={f.consumer_id} onChange={e=>setF({...f,consumer_id:e.target.value})}>{consumers.map(c=><option key={c.id} value={c.id}>{c.name} ({c.consumer_id})</option>)}</select></Field><Field label="Start date"><input required type="date" value={f.start_date} onChange={e=>setF({...f,start_date:e.target.value})}/></Field><Field label="End date"><input required type="date" min={f.start_date} value={f.end_date} onChange={e=>setF({...f,end_date:e.target.value})}/></Field><Field label="Amount"><input required type="number" min="0" step="0.01" value={f.amount} onChange={e=>setF({...f,amount:e.target.value})}/></Field><Field label="Payment status"><select value={f.payment_status} onChange={e=>setF({...f,payment_status:e.target.value})}><option value="pending">Pending</option><option value="paid">Paid</option><option value="partial">Partial</option></select></Field><button className="primary full">Save subscription</button></form></Modal>}
function PaymentModal({consumers,close,save}){const[f,setF]=useState({consumer_id:consumers[0]?.id||'',amount:'',method:'cash',reference:''});return <Modal title="Record payment" close={close}><form onSubmit={e=>{e.preventDefault();save(f)}}><Field label="Consumer"><select value={f.consumer_id} onChange={e=>setF({...f,consumer_id:e.target.value})}>{consumers.map(c=><option key={c.id} value={c.id}>{c.name} ({c.consumer_id})</option>)}</select></Field><Field label="Amount"><input required type="number" min="0.01" step="0.01" value={f.amount} onChange={e=>setF({...f,amount:e.target.value})}/></Field><Field label="Method"><select value={f.method} onChange={e=>setF({...f,method:e.target.value})}><option value="cash">Cash</option><option value="upi">UPI</option><option value="bank">Bank transfer</option><option value="card">Card</option></select></Field><Field label="Reference"><input value={f.reference} onChange={e=>setF({...f,reference:e.target.value})}/></Field><button className="primary full">Record payment</button></form></Modal>}
function QRModal({c,close}){const[data,setData]=useState(null),[err,setErr]=useState('');useEffect(()=>{api(`/api/v1/consumers/${c.id}/qr`).then(setData).catch(e=>setErr(e.message))},[c.id]);return <Modal title={`${c.name} · QR code`} close={close}>{err?<div className="error">{err}</div>:data?<div className="qr-card"><img alt="Consumer QR" src={`data:image/png;base64,${data.image_base64}`}/><b>{data.name}</b><small>{data.consumer_id}</small><small>{data.qr_token}</small></div>:<Empty text="Generating QR…"/>}</Modal>}
function Empty({text}){return <div className="empty">{text}</div>}createRoot(document.getElementById('root')).render(<App/>);
